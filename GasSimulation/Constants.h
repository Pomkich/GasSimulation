#pragma once

const double screen_width = 800;
const double screen_heigth = 600;
const double wall_size = 50;

const int molecules_size = 50;
const int max_velocity = 300;

enum class side {
	left = 0, up, rigth, bottom
};